from helper import *
import sys

def execute_select(joinTable, select_query, from_query, require_tables):
        agg_func = ['MAX', 'MIN', 'SUM', 'AVERAGE','DISTINCT','max','min','sum','average','distinct']

        
